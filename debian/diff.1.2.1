# Diff for generate 1.2.1 packages
Index: debian/changelog
===================================================================
RCS file: /home/cvs/gateway/debian/changelog,v
retrieving revision 1.8
diff -u -3 -p -r1.8 changelog
--- debian/changelog	16 Sep 2002 00:53:03 -0000	1.8
+++ debian/changelog	12 Jan 2003 19:53:44 -0000
@@ -1,4 +1,11 @@
-kannel (1.2.0-1) unstable; urgency=low
+kannel (1.2.1-0) unstable; urgency=low
+
+  * New upstream version
+  * Non-Official Package
+
+ -- Bruno Rodrigues <bruno.rodrigues@litux.org>  Sat, 11 Jan 2003 16:16:39 +0100
+
+kannel (1.2.0-0) unstable; urgency=low
 
   * New upstream version
   * Non-Official Package
Index: debian/control
===================================================================
RCS file: /home/cvs/gateway/debian/control,v
retrieving revision 1.2
diff -u -3 -p -r1.2 control
--- debian/control	26 Apr 2002 21:27:24 -0000	1.2
+++ debian/control	12 Jan 2003 19:53:44 -0000
@@ -3,6 +3,7 @@ Maintainer: Bruno Rodrigues <bruno.rodri
 Section: net
 Priority: optional
 Standards-Version: 3.1.1
+Build-Depends: gcc, libxml2-dev, libssl-dev, openssl, libmysqlclient10-dev, docbook-dsssl, jadetex, transfig, imagemagick, gs
 
 Package: kannel
 Architecture: any
Index: debian/postinst
===================================================================
RCS file: /home/cvs/gateway/debian/postinst,v
retrieving revision 1.5
diff -u -3 -p -r1.5 postinst
--- debian/postinst	11 Jun 2002 20:59:52 -0000	1.5
+++ debian/postinst	12 Jan 2003 19:53:44 -0000
@@ -6,8 +6,8 @@ set -e
 
 # Automatically added by dh_installdocs
 if [ "$1" = "configure" ]; then
-        if [ -d /usr/doc -a ! -e /usr/doc/aktion -a -d /usr/share/doc/aktion ]; then
-                ln -sf ../share/doc/aktion /usr/doc/aktion
+        if [ -d /usr/doc -a ! -e /usr/doc/kannel -a -d /usr/share/doc/kannel ]; then
+                ln -sf ../share/doc/kannel /usr/doc/kannel
         fi
 fi
 # End automatically added section
Index: debian/postrm
===================================================================
RCS file: /home/cvs/gateway/debian/postrm,v
retrieving revision 1.1
diff -u -3 -p -r1.1 postrm
--- debian/postrm	10 Oct 2000 13:43:56 -0000	1.1
+++ debian/postrm	12 Jan 2003 19:53:44 -0000
@@ -6,4 +6,7 @@ set -e
 
 if [ purge = "$1" ]; then
     update-rc.d kannel remove >/dev/null
+    test -d /var/log/kannel && rm -fr /var/log/kannel
+    test -d /etc/kannel && rmdir /etc/kannel
+    id kannel && userdel -r kannel
 fi
